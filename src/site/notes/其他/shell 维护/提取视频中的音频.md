---
{"dg-publish":true,"permalink":"/其他/shell 维护/提取视频中的音频/","tags":["ffmpeg"]}
---



```bash
for file in `ls *.{mp4,mov}(N)`; do ffmpeg -y -i "$file" -c:a libmp3lame -b:a 128k -q:a 0 -map a "${file%.*}.mp3"; done
```

